{
  "buildCommand": "cd frontend && pnpm install && pnpx prisma generate --schema=../prisma/schema.prisma && pnpm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "env": {
    "DATABASE_URL": "@database_url"
  }
}
